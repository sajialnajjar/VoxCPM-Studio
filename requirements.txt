# ╔══════════════════════════════════════════════════════════╗
# ║       VoxCPM Studio — Python Package Requirements       ║
# ║       Install inside the Python 3.10 venv ONLY          ║
# ╚══════════════════════════════════════════════════════════╝

# ── VoxCPM core (TTS engine) ──────────────────────────────
voxcpm

# ── GUI framework ─────────────────────────────────────────
customtkinter>=5.2.2
Pillow>=9.0.0

# ── Audio I/O ─────────────────────────────────────────────
soundfile>=0.12.1
numpy>=1.23.0

# ── Audio playback (optional but recommended) ─────────────
pygame>=2.5.0
sounddevice>=0.4.6

# ── ASR / Transcription (auto-fill reference transcript) ──
funasr>=1.1.0
modelscope>=1.9.0

# ── Fallback ASR (lighter alternative) ───────────────────
faster-whisper>=1.0.0

# ── Hugging Face model download ───────────────────────────
huggingface-hub>=0.23.0
